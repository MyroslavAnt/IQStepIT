// function isEven(number) {
//     return new Promise((resolve, reject) => {
//       typeof number !== 'number' ? reject('Введене значення не є числом') : resolve(number % 2 === 0 ? `${number} - парне число` : `${number} - не парне число`);
//     });
//   }

//   const num = 15;
  
//   isEven(num)
//     .then(result => console.log(result))
//     .catch(error => console.error(error));
  